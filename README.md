# DevOps Hands-on — Demo Repo

This repository contains a minimal ML classification Flask app, a small frontend, Dockerfile, GitHub Actions workflow, Terraform infra code (ECR + ECS + ALB), and monitoring (Prometheus + Grafana) docker-compose config.

Follow the included PowerShell script `setup_demo.ps1` to run the local setup steps.
