global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'ml-app'
    metrics_path: /metrics
    static_configs:
      - targets: ['flask_app:5000']  # replace <ALB_DNS> with the DNS name from Terraform output
